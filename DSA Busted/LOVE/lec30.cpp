#include<bits/stdc++.h>
using namespace std;
#define pi 3.14
#define max(a, b) (a>b)? a:b

#define INSTAGRAM FOLLOWERS
#define FOLLOWERS 138

inline double areaCircle(int x = 2){
    x = pi * x * x;
    int result = x;
    return result;
}

// inline bool checkGreat(int x){
//     return (x < 30) ? true : false;
// }


int main()
{
    double n;
    n = areaCircle();
    cout<<n;
    // cout<<endl<<checkGreat(n);
    int x = max(10, 20);
    cout<<x;
    cout<<INSTAGRAM;
    return 0;
}